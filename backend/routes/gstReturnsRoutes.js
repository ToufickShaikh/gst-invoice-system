const express = require('express');
const router = express.Router();

// This file is intentionally left blank.
// The routes have been consolidated into gstRoutes.js and gstService.js
// to create a single source of truth for GST data.

module.exports = router;